---
title: PDFTypography
description: PDFHeading and text primitives with a shared type scale.
createdAt: 2026-08-01
updatedAt: 2026-09-11
---

<ComponentPreview name="typography">

```tsx
import { PDFDocument, PDFPage } from "@/components/pdf/document";
import { PDFContainer, PDFHeading, PDFText } from "@/components/pdf";

export default function TypographyDemo() {
  return (
    <PDFDocument title="Typography Demo" author="shadcn-pdf">
      <PDFPage>
        <PDFContainer className="flex-col gap-2">
          <PDFHeading level={1}>Heading 1</PDFHeading>
          <PDFHeading level={2}>Heading 2</PDFHeading>
          <PDFHeading level={3}>Heading 3</PDFHeading>
          <PDFText>Body text with the shared type scale.</PDFText>
          <PDFText variant="small" color="#737373">Small muted text.</PDFText>
        </PDFContainer>
      </PDFPage>
    </PDFDocument>
  );
}
```

</ComponentPreview>

## Features

- `PDFHeading` — semantic heading levels (1–4) from the theme type scale.
- `PDFText` — body, small, and mono text variants.
- Supports color and alignment overrides per instance.
- Supports `className` prop for Tailwind-style overrides.

## Installation

<CodeTabs>

<TabsListInstallType />

<TabsContent value="cli">

```bash
npx shadcn@latest add sammykad/shadcn-pdf/typography
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the dependency</Step>

```bash
npm install @react-pdf/renderer
```

<Step>Copy and paste the following code into your project</Step>

<ComponentSource name="typography" title="components/pdf/typography.tsx" />

<Step>Update the import paths to match your project setup</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { PDFDocument, PDFPage } from "@/components/pdf/document";
import { PDFHeading, PDFText } from "@/components/pdf";
import { renderToBuffer } from "@react-pdf/renderer";

function MyDocument() {
  return (
    <PDFDocument title="My Document" author="Me">
      <PDFPage>
        <PDFHeading level={2}>Section heading</PDFHeading>
        <PDFText variant="small" color="#737373">Supporting detail</PDFText>
      </PDFPage>
    </PDFDocument>
  );
}

// Render to buffer
const buffer = await renderToBuffer(<MyDocument />);
```

## With className

```tsx
<PDFHeading level={1} className="text-4xl font-bold">Large title</PDFHeading>
<PDFText className="text-muted-foreground">Muted text</PDFText>
```

## API reference

### PDFHeading

| Prop | Type |
| --- | --- |
| children | `ReactNode` |
| level? | `1 \| 2 \| 3 \| 4` |
| color? | `string` |
| align? | `"left" \| "center" \| "right"` |
| className? | `string` |
| style? | `Style` |

### PDFText

| Prop | Type |
| --- | --- |
| children | `ReactNode` |
| variant? | `"small" \| "mono"` |
| color? | `string` |
| align? | `"left" \| "center" \| "right"` |
| className? | `string` |
| style? | `Style` |

## Credits

- [shadcn/ui](https://ui.shadcn.com)
