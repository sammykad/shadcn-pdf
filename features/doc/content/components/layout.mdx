---
title: Layout
description: PDFContainer for flex-based layouts with Tailwind className support.
createdAt: 2026-08-01
updatedAt: 2026-09-13
---

## What is Layout?

Layout in shadcn-pdf is handled by `PDFContainer` — a single component that wraps react-pdf's `View` with Tailwind `className` support. Instead of learning multiple layout primitives (Stack, Row, Grid), you use one component with Tailwind classes.

## When to use it

- **Vertical stacking** — `className="flex-col gap-2"`
- **Horizontal rows** — `className="flex-row gap-2"`
- **Space between** — `className="flex-row justify-between"`
- **Grids** — `className="flex-row flex-wrap gap-2"` with child widths
- **Any flexbox layout** you'd build with CSS in the browser

```tsx
import { PDFText, PDFContainer } from "@/components/pdf";

export default function PDFLayoutDemo() {
  return (
    <PDFContainer className="flex-col gap-3 p-4">
      <PDFContainer className="flex-row justify-between items-center bg-zinc-50 rounded-lg p-3">
        <PDFText className="text-sm font-medium text-foreground">Left</PDFText>
        <PDFText className="text-sm font-medium text-foreground">Right</PDFText>
      </PDFContainer>
      <PDFContainer className="flex-row flex-wrap gap-3">
        <PDFContainer className="w-[30%] bg-zinc-50 rounded-lg p-3">
          <PDFText className="text-xs text-center text-zinc-500">Cell 1</PDFText>
        </PDFContainer>
        <PDFContainer className="w-[30%] bg-zinc-50 rounded-lg p-3">
          <PDFText className="text-xs text-center text-zinc-500">Cell 2</PDFText>
        </PDFContainer>
        <PDFContainer className="w-[30%] bg-zinc-50 rounded-lg p-3">
          <PDFText className="text-xs text-center text-zinc-500">Cell 3</PDFText>
        </PDFContainer>
      </PDFContainer>
    </PDFContainer>
  );
}
```

## Why no PDFStack / PDFRow / PDFGrid?

These were thin wrappers around `View`. Since `PDFContainer` is already simple and Tailwind handles all the styling, we removed the extra abstractions — just like shadcn/ui does with HTML elements.

## Installation

No installation needed — `PDFContainer` is already part of `@/components/pdf`.

```bash
npm install @react-pdf/renderer
```

## Credits

- [shadcn/ui](https://ui.shadcn.com)
